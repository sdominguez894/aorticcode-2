/**
 * Enumeration for branch sides
 */
export const BranchSide = Object.freeze({
    IPSILATERAL: 'IPSILATERAL',      // Same side as main body insertion
    CONTRALATERAL: 'CONTRALATERAL'   // Opposite side
});
